// Este archivo se sustituyó por alertWatcher.js, que además de mandar a
// Telegram agrupa alertas repetidas (correlación) y dispara el análisis
// automático con IA para las críticas. No se usa desde ningún sitio —
// server.js ya importa startAlertWatcher() de ./alertWatcher.js.
export {};
